name: flutter_tdd_example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  app_color: ^0.0.7
  flutter_app_navigator: ^1.0.1
  flutter_androssy: ^0.2.7
  get_it: ^7.6.4
  equatable: ^2.0.5
  dartz: ^0.10.1
  http: ^1.1.0
  bloc: ^8.1.2
  meta: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  mocktail: ^1.0.1

flutter:

  uses-material-design: true